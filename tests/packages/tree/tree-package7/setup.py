from setuptools import setup, find_packages

install_requires = [
    'tree-package8'
]

setup(
    name='tree-package7',
    version='1.0.0',
    install_requires=install_requires,
    packages=find_packages(),
)
